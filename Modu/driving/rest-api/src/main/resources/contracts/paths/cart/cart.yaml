get:
  tags:
    - Cart
  summary: Displays the user's cart
  description: Displays the user's cart details and contained items, if any
  operationId: getValidatedCart
  security:
    - deviceAuth: []

  responses:
    '200':
      description: Cart retrieved successfully
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/CartResponse.yaml'
    '401':
      description: User authentication by device IMEI is missing.
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/ErrorResponse.yaml'
    '404':
      description: Requested cart could not be found
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/ErrorResponse.yaml'
