get:
  tags:
    - Models
  summary: Get models (paged and filtered if requested)
  description: Return a list of all models, paged and filtered (if requested).
  operationId: getModels

  responses:
    '200':
      description: List of available categories
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../components/schemas/HomeCategoryResponse.yaml'
